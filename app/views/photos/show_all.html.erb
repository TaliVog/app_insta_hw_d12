<% @photos.each do |photo| %></br>
	<%= photo.caption %></br>
	<img src=<%= photo.image %> > </br>
	<%= link_to 'Show', user_photo_path(current_user, photo) %>
<% end %>

<%= link_to "My Photos", user_photos_path(current_user) %>